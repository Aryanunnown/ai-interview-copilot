// Unit test placeholders:
// - parses valid Groq JSON into a normalized resume profile
// - rejects invalid JSON so upload can fall back to deterministic parsing
// - normalizes skill aliases, duplicates, and sorted list output

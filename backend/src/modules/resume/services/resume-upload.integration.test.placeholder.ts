// Integration test placeholders:
// - POST /resume/upload persists AI-extracted profile fields when Groq succeeds
// - POST /resume/upload falls back to deterministic parsing when Groq fails
// - POST /resume/upload response includes the resume intelligence summary fields

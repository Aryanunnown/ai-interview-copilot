export const resumeUploadTestPlaceholders = [
  'POST /api/resume/upload rejects unauthenticated requests',
  'POST /api/resume/upload rejects missing files',
  'POST /api/resume/upload rejects unsupported file types',
  'POST /api/resume/upload extracts and stores PDF resume text',
  'POST /api/resume/upload extracts and stores DOCX resume text',
  'POST /api/resume/upload returns resumeId, skills, experienceYears, and domain',
];
